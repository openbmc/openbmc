#!/bin/sh

find ./ -name Makefile | while read -r x; do
    make -C "$(dirname "${x}")" -k runtest-TESTS | sed \
        -e 's/^\(\[       OK \].*\)$/PASS: \1/' \
        -e 's/^\(\[  FAILED  \].*\)$/FAIL: \1/' \
        -e 's/^\(\[  SKIPPED \].*\)$/SKIP: \1/'
done
