[Unit]
Description=Phosphor Settings Daemon
Before=mapper-wait@-.service

[Service]
ExecStart={sbindir}/phosphor-settings-manager
Restart=always
Type=dbus
BusName={BUSNAME}

[Install]
WantedBy={SYSTEMD_DEFAULT_TARGET}
