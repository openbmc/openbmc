[Unit]
Description=FRU Fault monitor service
Wants=mapper-wait@-xyz-openbmc_project-LED-GroupManager.service
After=mapper-wait@-xyz-openbmc_project-LED-GroupManager.service

[Service]
Restart=always
ExecStart={sbindir}/phosphor-fru-fault-monitor

[Install]
WantedBy={SYSTEMD_DEFAULT_TARGET}
