[Unit]
Description=Phosphor OpenBMC event management daemon

[Service]
EnvironmentFile=-/etc/eventd.conf
Restart=always
ExecStart=/usr/sbin/obmc-phosphor-eventd $OPTIONS -t 128

[Install]
WantedBy=multi-user.target
