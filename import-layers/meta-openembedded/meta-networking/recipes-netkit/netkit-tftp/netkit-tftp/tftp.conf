service tftp
{
    disable        = no
    socket_type    = dgram
    port           = 69
    protocol       = udp
    wait           = yes
    user           = nobody
    server         = /usr/sbin/in.tftpd
    server_args    = /tftpboot
}
